# AGENTS

## Public Dashboard Boundary

This repo is the public deployment target only. The canonical dashboard UI, HTML, summary JSON, and bundle manifest are generated by:

```text
D:\projects\Alpha Research Ops Lab\alpha_research_ops\beginner_research_dashboard.py
```

Do not directly edit public dashboard UI in `index.html`. Root `index.html`, `BEGINNER_RESEARCH_DASHBOARD_SUMMARY.json`, and `DASHBOARD_BUNDLE_MANIFEST.json` are generated copy artifacts from Alpha.

Runner/backend files may be edited here when the task requires it:

```text
scripts\external_strategy_batch.py
workers\strategy-dispatch-worker.js
tests\
.github\
wrangler.strategy-dispatch.toml
```

If a visible dashboard contract changes, update Alpha generator and Alpha focused dashboard tests first. Then generate the Alpha bundle, copy the generated files into this repo, and run the sync verifier.

## Required Gate Before Public Commit Or Deploy

```text
py -m py_compile scripts\external_strategy_batch.py
py -m pytest -q tests\test_external_strategy_batch.py
node --check workers\strategy-dispatch-worker.js
py scripts\verify_dashboard_bundle_sync.py --alpha-bundle "D:\projects\Alpha Research Ops Lab\artifacts\beginner_research_dashboard\<run_id>" --public-root .
```

Public deploy is blocked when Alpha focused dashboard tests fail, the generated manifest is missing, or the sync verifier reports public-only HTML/summary/manifest drift.
